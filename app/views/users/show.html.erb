<h1><%= current_user.name %></h1>
<h3>Reports: </h3>
<% if current_user.reports.empty? %>
    <p>No reports yet</p>
<% else %>
    <% current_user.reports.each do |report| %>
        <p><%= report.date %> - <%= report.client.name %> - <%= report.rating %></p>
    <% end %>
<% end %>
