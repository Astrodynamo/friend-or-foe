<%= form_for :user, url: '/login' do |f| %>
    <%= f.label :name %>
    <%= f.collection_select :id, User.all, :id, :name %><br>

    <%= f.label :password %>
    <%= f.password_field :password %><br>

    <%= f.submit "Log In"%>
<% end %>